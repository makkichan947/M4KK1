# M4KK1内核配置文件
# 定义内核构建配置选项

# 内核版本信息
KERNEL_VERSION := 0.1.0
KERNEL_CODENAME := "M4KK1"
KERNEL_AUTHOR := "makkichan947"

# 导出配置变量
export KERNEL_VERSION KERNEL_CODENAME KERNEL_AUTHOR

# 架构配置
CONFIG_ARCH := m4kk1
CONFIG_ARCH_M4KK1 := y

# 内核特性配置
CONFIG_MONOLITHIC_KERNEL := y
CONFIG_MODULAR := y
CONFIG_SMP := n
CONFIG_PREEMPT := y

# 文件系统配置
CONFIG_YFS := y
CONFIG_SWAP2 := y
CONFIG_PROCFS := y

# 设备驱动配置
CONFIG_VGA_CONSOLE := y
CONFIG_SERIAL_CONSOLE := n
CONFIG_KEYBOARD := y
CONFIG_MOUSE := y
CONFIG_ATA := y
CONFIG_USB := y
CONFIG_NETWORK := y
CONFIG_PCI := y

# 内存管理配置
CONFIG_MMU := y
CONFIG_PAGING := y
CONFIG_DEMAND_PAGING := y

# 导出所有配置
export CONFIG_ARCH CONFIG_ARCH_M4KK1
export CONFIG_MONOLITHIC_KERNEL CONFIG_MODULAR CONFIG_SMP CONFIG_PREEMPT
export CONFIG_YFS CONFIG_SWAP2 CONFIG_PROCFS
export CONFIG_VGA_CONSOLE CONFIG_SERIAL_CONSOLE CONFIG_KEYBOARD CONFIG_MOUSE
export CONFIG_ATA CONFIG_USB CONFIG_NETWORK CONFIG_PCI
export CONFIG_MMU CONFIG_PAGING CONFIG_DEMAND_PAGING